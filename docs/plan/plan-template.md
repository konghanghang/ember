# <功能名称>实现方案

> 状态：草稿
> 负责人：<姓名或角色>
> 更新时间：<YYYY-MM-DD>

## 背景

这个问题为什么现在要解决：

- <真实问题 1>
- <真实问题 2>
- <如果不做，会产生什么影响>

## 目标

本方案要实现：

1. <目标 1>
2. <目标 2>
3. <目标 3>

## 非目标

本次明确不做：

- <不做的事 1>
- <不做的事 2>

## 当前事实

以当前代码和现行文档为准，先写清现状：

- 相关文档：<路径>
- 相关服务/页面/模型：<标识符或路径>
- 当前行为：<一句话说清>
- 现有限制：<一句话说清>

## 方案设计

### 1. 用户可见行为

- 新增什么能力
- 修改什么现有行为
- 哪些现有行为必须保持不变

### 2. 数据与模型

- 新增字段 / 表 / 配置项
- 修改哪些现有结构
- 是否需要迁移

如果没有数据结构变化，直接写：

> 本次不涉及数据模型变更。

### 3. 接口与边界

- 新增或修改哪些 API / Internal API / webhook / 命令
- 请求参数与响应字段怎么变
- 哪些调用方会受影响

### 4. 关键流程

按顺序写清主链路，不需要贴代码：

1. <步骤 1>
2. <步骤 2>
3. <步骤 3>

### 5. 失败路径与边界条件

- <异常场景 1>：<系统应如何表现>
- <异常场景 2>：<系统应如何表现>
- <兼容性约束>：<不能破坏什么>

## 影响范围

涉及的子系统：

- API：<有/无，涉及什么>
- Web：<有/无，涉及什么>
- Bot：<有/无，涉及什么>
- 配置/部署：<有/无，涉及什么>
- 文档：<要更新哪些现行文档>

## 验证方式

### 编译/测试

- `cd services/api && go test ./...`
- `cd services/api && go build ./...`
- `cd services/web && npm run build`
- `cd services/bot && python -m py_compile main.py`

按需删除不相关项，不要保留无意义命令。

### 手工验证

- <场景 1>
- <场景 2>
- <场景 3>

## 落地后文档处理

落地后应同步处理：

- 提炼哪些稳定结论到 `docs/reference/` 或 `docs/system-architecture.md`
- 这份方案何时移入 `docs/archive/`
