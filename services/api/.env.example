# ==================== API 服务配置（仅保留当前代码实际使用项） ====================

# 服务端口（可选，默认 8080）
PORT="8080"

# PostgreSQL 连接串（必填）
DATABASE_URL="postgresql://postgres:password@localhost:5432/ember?search_path=public"

# JWT 密钥（必填，至少 32 字符）
JWT_SECRET="your-secret-key-min-32-chars-change-this-in-production"

# 是否启动时自动迁移（可选，默认 false）
AUTO_MIGRATE="false"

# 默认管理员初始化（可选）
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="change-this-admin-password"

# Emby 配置（必填）
EMBY_URL="https://your-emby-server.com"
EMBY_API_KEY="your-emby-api-key"

# 前端展示用 Emby 地址（可选，不配置时回退到 EMBY_URL）
NEXT_PUBLIC_EMBY_URL="https://your-emby-server.com"

# TMDB 搜索（必填）
TMDB_API_KEY="your-tmdb-api-key"

# MoviePilot（可选，不配置则审核通过时跳过 MP 调用）
MOVIEPILOT_URL="http://your-moviepilot-server:3001"
MOVIEPILOT_USERNAME="admin"
MOVIEPILOT_PASSWORD="your-moviepilot-password"

# Cron（可选）
CRON_ENABLED="true"
CRON_SCHEDULE="0 2 * * *"
CRON_TIMEZONE="Asia/Shanghai"

# 内部服务通信（Bot <-> API）
INTERNAL_API_SECRET="your-internal-api-secret"

# Bot 通知地址（可选，不配置则不发订阅通知）
BOT_NOTIFY_URL="http://localhost:8000"

